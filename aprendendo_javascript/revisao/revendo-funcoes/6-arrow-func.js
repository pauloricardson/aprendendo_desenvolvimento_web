const soma = (v1, v2) => {
    return v1 + v2;
}

console.log(soma(10, 10));

const nome = n => {
    return n;
}

const adicionar = n => n + 10;

console.log(adicionar(10));

const soma2 = (v1, v2) => v1 + v2;

const soma3 = (v1, v2) => {
    let res = v1 + v2;
    return res;
}

console.log(soma3(35, 12))

const text = t => t;

console.log(text("olá"));